interface:
  display_name: "Option Strategy Builder"
  short_description: "Build option strategy schema, persistence, gateways, and signals"
  default_prompt: "Use $build-option-strategy to design or implement an option strategy system with reusable schema, persistence, contract filtering, and signal workflows."
